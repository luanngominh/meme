---
- host: influx
  become: true
  roles:
    - basic
    - ntp
    # - influxdb

# Install checkMK cluster
- host: checkmk
  become: true
  roles:
    - basic
    - ntp
    # more role such as DRDB, CoreSync and so on
    # - checkmk

